services:
  postgres:
    ports:
      - "5432:5432"
  frontend:
    environment:
      - REACT_APP_API_URL=http://localhost:${PORT_BACK:-8080}
    build:
      args:
        - REACT_APP_API_URL=http://localhost:${PORT_BACK:-8080}
networks:
  devops_network:
    external: false
  database-network:
    internal: false